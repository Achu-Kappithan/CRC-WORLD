
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Placed Successfully!</title>
    <style>
        body {
            font-family: 'Poppins', Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f9;
            overflow: hidden;
            position: relative;
        }
        .container {
            text-align: center;
            background-color: #ffffff;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            position: relative;
            animation: fadeIn 1s ease-in-out;
        }
        h1 {
            color: #4CAF50;
            margin-bottom: 1rem;
            font-size: 2rem;
        }
        p {
            margin: 0.5rem 0;
            font-size: 1.1rem;
            color: #555;
        }
        .order-details {
            margin: 1.5rem 0;
            font-size: 1.2rem;
            color: #333;
        }
        .buttons {
            margin-top: 2rem;
        }
        .buttons a {
            text-decoration: none;
            padding: 0.8rem 2rem;
            border-radius: 8px;
            background-color: #4CAF50;
            color: white;
            margin: 0 0.5rem;
            font-size: 1rem;
            transition: background-color 0.3s ease-in-out;
        }
        .buttons a:hover {
            background-color: #388E3C;
        }
        .checkmark {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            display: block;
            stroke-width: 2;
            stroke: #4CAF50;
            stroke-miterlimit: 10;
            margin: 20px auto;
            box-shadow: inset 0px 0px 0px #4CAF50;
            animation: fill .4s ease-in-out .4s forwards, pulse 1.2s infinite ease-in-out;
        }
        .checkmark__circle {
            stroke-dasharray: 166;
            stroke-dashoffset: 166;
            stroke-width: 2;
            stroke-miterlimit: 10;
            stroke: #4CAF50;
            fill: none;
            animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
        }
        .checkmark__check {
            transform-origin: 50% 50%;
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
        }
        @keyframes stroke {
            100% {
                stroke-dashoffset: 0;
            }
        }
        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }
        @keyframes fill {
            100% {
                box-shadow: inset 0px 0px 0px 30px #4CAF50;
            }
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background-color: #f0f0f0;
            top: -10px;
            animation: confetti 5s ease-in-out infinite;
            opacity: 0.8;
        }
        @keyframes confetti {
            0% {
                transform: translateY(0) rotateZ(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(1000px) rotateZ(720deg);
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Checkmark icon -->
        <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
            <circle class="checkmark__circle" cx="26" cy="26" r="25" fill="none"/>
            <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
        </svg>

        <!-- Order placed successfully message -->
        <h1>Order Placed Successfully!</h1>
        <p>Thank you for your purchase. Your order is being processed.</p>

        <!-- Order details section -->
        <div class="order-details">
            <p><strong>Order ID:</strong> <%= newOrder._id %></p>
            <p><strong>Total Amount:</strong> ₹<%= newOrder.totalPrice %></p>
        </div>

        <!-- Action buttons -->
        <div class="buttons">
            <a href="/user_shop" id="continueShopping">Continue Shopping</a>
            <a href="/order_summary?orderid=<%= newOrder._id %>" id="viewOrderSummary">View Order Summary</a>
            <a href="/my_orders" id="myOrders">Go to My Orders</a> <!-- New button -->
        </div>
    </div>

    <!-- Confetti celebration -->
    <script>
        function createConfetti() {
            const confetti = document.createElement('div');
            confetti.classList.add('confetti');
            confetti.style.left = Math.random() * 100 + 'vw';
            confetti.style.animationDuration = Math.random() * 3 + 2 + 's';
            confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 100%, 50%)`;
            document.body.appendChild(confetti);

            setTimeout(() => {
                confetti.remove();
            }, 5000);
        }

        // Create confetti every 80ms
        setInterval(createConfetti, 80);
    </script>
</body>
</html>

